## Page-5 competing-model chart — plan

**Goal (round-3 recovery #4, owed twice):** make page 5's figure *show* the contest the caption and dossier (d-0002) describe. Currently it fixes one ~$100k band and draws essentially one curve; the owed fix is to render the competing model shapes.

### Decision (form follows content — R-V3; R-V5: a form must express tension)

**Overlaid competing curves**, keeping the `threshold-curve` form. Two labelled curves over a shared log-income axis, both rising together below ~$100k (the one thing all models agree on), then diverging:
- **Solid curve — "flattening ~$100k"** (the KKM 2023 / Rohrer-Wenz 2024 lower-conditional-quantile reading: rises, then the lower quantiles flatten).
- **Dashed curve — "plateau ~$200k"** (the Bennedsen 2024 reading: keeps rising, then plateaus later/higher).
- A **contested band spanning $100k–$200k** replaces the single-point band, labelled "where the flattening sits depends on the model — and whether it sits anywhere is contested."

This is the most faithful to d-0002's `findings` (three model results) and `dissent` ("the threshold's location is model-dependent and contested"). It does NOT change the assertions (a-0024/a-0025) or the dossier — purely the figure. Throwing away the curve-form (the panel option) would over-react: the dose-response below the band IS agreed, and R-V5 asks the form to carry the tension, not be replaced by it.

### Honesty guardrails for the figure (from d-0002 permitted/prohibited inferences)
- The figure is **schematic** — model *directions*, not plotted data points. The caption already says so; keep it.
- Both curves must rise with **log-income** below the band (the permitted inference: "experienced well-being tracks log-income; a strong 'money doesn't matter' reading fails").
- No curve may read as "the" result — both are one model each, labelled, with the band showing the disagreement.
- The **lower-quantile** subtlety stays in the label/caption, not misrepresented as a subgroup of people (the round-3 P0-7 error, now fixed in a-0024; the figure label must not regress to "unhappy people").
- No causal claim (observational).

### Steps
1. **Rewrite `form_threshold_curve` in `build_book.py`** to accept competing-model config: two curves (each with a breakpoint + post-breakpoint slope) over the shared axis, a wide contested band, and per-curve labels. Keep it backward-aware (the SPEC wants this form reusable). The current renderer hardcodes one solid + one dashed curve already; I'll generalize so the two curves are driven by config (`curves:` list with `breakpoint`, `after_slope`, `style`, `label`) plus a `contested_band: {from, to, label}`. Schematic — no data coordinates, just honest model shapes.

2. **Rewrite `by-050-money.yaml` copy** to drive the new renderer: replace the single `band_label`/`main_label`/`minority_label` with the two-model `curves` config + the wide contested-band label. Keep the intro (a-0023), caption (editorial schematic-note + a-0024), outro (a-0025), the verbatim Schopenhauer quote (c-0013), and the axis labels. The caption's "schematic curves — one model's direction, not plotted data" line becomes "schematic curves — competing models' directions, not plotted data."

3. **Validate + render + QA**: graph + units validators; render → PDF → **extract page-5 text** (the vision tool returned cached URLs last time, so I'll verify via pypdfium text extraction, which is definitive) → confirm both curves + band + labels render, the page fits one page, and page 4 (just redesigned) + page 6 still render clean. Run fixture + gate tests. No assertion text changes, so no re-sign needed — but I'll verify the manifest still matches.

4. **Adjudication note**: append a short record to the existing `ops/adjudications/2026-07-07-gateE-by040-050.md` (or a new dated file) documenting the chart-form change and the honesty guardrails, closing the "owed: competing-model chart" item recorded there and in d-0002.

5. **Update STATE.md** next-action #4 (mark the page-5 chart piece done) + session log; commit + push.

### Not in scope
a-0024/a-0025/d-0002 content (unchanged — the chart visualizes what they already say). The remaining #4 items (fresh adversarial passes on pages 2/7/8) stay queued.